# Redirect legacy /demofox URLs to the subdomain.
# Include in yuri.guru server block BEFORE location / { ... }

location ~ ^/demofox/?(.*)$ {
    return 301 https://foodfox.yuri.guru/$1$is_args$args;
}
